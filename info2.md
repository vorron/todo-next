Проблемные зоны:

Нарушена принципиальная чистота слоёв FSD (особенно entities содержит UI).
Нет архитектурного контракта между слоями → зависимость от RTK Query «вплетена» во всю логику.
screens дублирует features → неясна их взаимосвязь и цели.
Защита маршрутов реализована фрагментарно (мидлварь + AuthGuard + условный рендер).
Отсутствует единая точка работы с сессией → логика размазана по useAuth, sessionStorage, useSessionCookie.
Нет поддержки type-safe layout routing → сложнее масштабироваться.
2. 📐 Концептуальные принципы архитектуры
Для достижения максимальной масштабируемости и поддерживаемости, мы применяем следующие принципы:

Принцип
Обоснование
Чистота слоёв FSD
Каждый слой имеет единственную ответственность. entities — только данные и чистые функции. UI — только в features/widgets/screens.
UI ≠ domain logic
Компоненты не должны зависеть от RTK Query напрямую. Абстрагируем через хуки и сервисы.
Layout-группировка
Public/protected зоны через Next.js route groups: (public), (protected).
Единая точка авторизации
Выносим работу с сессией в entities/session или lib/session.
Минимизация проектных знаний
Архитектура должна быть настолько ясной, чтобы новый разработчик понял всё без onboarding.
Поддержка мета-архитектуры
Возможность вынести shared + config + base-layouts в корпоративный @company/foundation.
3. 🧱 Переработка слоёв по домену (todo в качестве примера)
3.1 entities/todo
Цель: Чистая доменная модель. Без React, Redux, UI.

✅ Должно содержать:

types.ts, schema.ts, constants.ts
Чистые утилиты (lib/): isTodoOverdue, formatDueDate и т.д.
Без компонентов (ui/), без хуков, без RTK
❌ Убрать:

ui/todo-card.tsx, ui/todo-status-badge.tsx → переместить в features/todo/ui
Почему? entities — это модель данных, а не представление. UI не должен «жить» там.

3.2 features/todo
Цель: Бизнес-логика + UI, ориентированный на действие.

✅ Должно содержать:

Хуки: useTodos, useCreateTodo, useOptimisticToggle
Компоненты: TodoCard, CreateTodoForm, DeleteTodoDialog
Слайсы (если есть локальное состояние)
Использовать RTK Query только через хуки → не напрямую в UI
Это единственное место, где допустимо смешивать логику и UI.

3.3 screens
Цель: Композиция виджетов/фич. НЕ логика, НЕ стили, НЕ данные.

✅ Должно содержать:

Только Page компоненты: TodosPage, TodoDetailPage
Импорты из features и widgets
Никакой авторизации (AuthGuard → в layout)
Никаких useQuery, useMutation → только презентация
Скрин — это шаблон страницы, а не место для логики.

3.4 widgets
Цель: Кросс-доменные UI-блоки, повторно используемые в разных экранах.

✅ Примеры:

Navbar
TodoList — но только как композиция features/todo
UserMenu
⚠️ Важно: widgets не должны содержать доменную логику.
→ TodoList должен принимать todos и actions как props, а не использовать useGetTodosQuery напрямую.

Сейчас widgets/todo-list нарушает это правило — рефакторинг необходим.

3.5 shared
Цель: Инфраструктура, не зависящая от бизнеса.

✅ Уже сделано хорошо:

ui/ — базовые компоненты
lib/ — утилиты, хуки, валидация
config/ — env, routes
✅ Рекомендуется:

Вынести layout базовые компоненты: BaseLayout, AuthLayout, DashboardLayout
4. 🔐 Авторизация: от мока к шаблону
Текущая реализация — хороший мок, но требует унификации.

Рекомендуемая структура:
ts
123456
Что делает session-service:
Читает из localStorage → валидирует через sessionSchema
Синхронизирует с document.cookie (или в будущем — cookies() Next.js)
Экспортирует getSession(), hasValidSession(), clearSession()
Где используется:
useAuth → вызывает sessionService
middleware → вызывает sessionService.hasValidSession()
useSessionCookie → вызывает sessionService.syncWithCookie()
Это даст готовый паттерн замены мока на JWT/NextAuth/Auth0 без изменения остального кода.

5. 🗺️ Маршрутизация и Layout
Текущая проблема:
Защита маршрутов «размазана» → сложно масштабировать.

Решение: Route Groups + Layout-based Auth
txt
123456789
(protected)/layout.tsx:
На сервере: проверяет сессию через sessionService
Если нет → redirect('/login')
На клиенте: AuthGuard как fallback (для SPA-переходов)
Middleware остаётся, но только как «last line of defense».

6. 🧬 Архитектурная независимость от RTK Query
Сейчас useGetTodosQuery используется напрямую в UI → сильная связка.

Решение: абстракция через useTodos (уже есть!)
Но:

widgets/todo-list использует useGetTodosQuery → нарушение.
Даже features/todo/model/use-todos.ts — зависит от RTK.
Идеальный подход:
ts
1234567891011
→ Это не обязательно сейчас, но заложите паттерн: RTK Query только в api/ или service/, а хуки работают с сервисом.

Это позволит в будущем заменить RTK на TanStack Query или Apollo без изменения UI.

7. 🧩 Поддержка «корпоративного foundation»
Ваши размышления абсолютно верны. Проект можно разделить на:

txt
123456789101112
Преимущества:
Новые проекты создаются за 1 день.
Обновление foundation → автоматически везде.
Единый UX, архитектура, безопасность.
Рекомендация: уже сейчас выделите shared + config + auth + layouts в отдельную директорию foundation/ (локально), чтобы понять границы.

🛠 План рефакторинга
Этап
Задача
Приоритет
1
Перенос UI из entities в features
🔴 Высокий
2
Введение route groups (public) / (protected)
🔴 Высокий
3
Создание sessionService и унификация работы с сессией
🔴 Высокий
4
Перенос AuthGuard из screens в (protected)/layout.tsx
🟠 Средний
5
Рефакторинг widgets/todo-list: убрать прямой вызов RTK Query
🟠 Средний
6
Внедрение type-safe маршрутизации с layout и auth флагами
🟠 Средний
7
Настройка next.config.ts + tsconfig + арх-линтер (eslint-plugin-fsd)
🟢 Низкий
8
Подготовка foundation/ структуры (локально)
🟢 Низкий